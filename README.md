# GTA Radial Menu

Standalone Vue/Vite demo of the GTA-style radial menu.

Local commands:

```bash
npm install
npm run dev
npm run build
```

GitHub Pages is configured through `.github/workflows/deploy.yml`.
